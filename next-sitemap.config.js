/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://ipoanalyser.in/',
    generateRobotsTxt: true, // (optional) will create robots.txt file too
    sitemapSize: 7000,        // optional, for big sites
  };
  